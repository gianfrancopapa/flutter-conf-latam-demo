name: user_repository_macros
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.5.3

dependencies:
  equatable: ^2.0.5
  json: ^0.20.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
